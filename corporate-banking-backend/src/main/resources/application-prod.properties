spring.application.name=corporate-banking-backend

# Production MongoDB configuration
spring.data.mongodb.uri=mongodb://localhost:27017/corporatebank

# Server configuration
server.port=9090
server.address=0.0.0.0

# Logging
logging.level.org.springframework.security=INFO

# Kafka configuration (disabled for production deployment)
spring.kafka.listener.auto-startup=false

# CORS configuration for production
spring.web.cors.allowed-origins=http://34.235.87.177,http://localhost:4200
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
